CC=gcc
LCURSES=-lncurses

ALL=init_players

USE_FILE=init_players.o use_curses.o init_player.o

all: ${ALL}

init_players: ${USE_FILE}
	${CC} -o $@ $^ ${LCURSES}

.c.o:
	${CC} -c $<

clean:
	rm -f *.o ${ALL}
